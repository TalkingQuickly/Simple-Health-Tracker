<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Simple Health Tracker</title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <!-- Le styles -->
    <%= stylesheet_link_tag    "application", :media => "all" %>
     <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
    <style>
      body {
        padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
      }
    </style>

  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
          <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="i-bar"></span>
            <span class="i-bar"></span>
            <span class="i-bar"></span>
          </a>
          <a class="brand" href="/">simple health tracker</a>
          <div class="container nav-collapse">
            <ul class="nav">
                <li><%= link_to "dashboard", "/dashboard"  %></li>
                <li><%= link_to "new entry", new_day_path  %></li>
                <li><%= link_to "about", "/about"  %></li>
                <% if !user_signed_in? %>
                	<li><%= link_to "login", "/users/sign_in"  %></li>
                <% else %>
             		<li><%= link_to("logout", destroy_user_session_path, :method => :delete) %></li>
                <% end %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
        <div class="content">
          <div class="row">
            <div class="span9">
            <%= yield %>
            </div>
            <div class="span1">&nbsp;</div>
            <div class="span3">
              <div class="well sidebar-nav">
                <h3>stuff to do</h3>
                 <ul class="nav nav-list">
                    <li><%= link_to "goto Dashboard", "/dashboard"  %></li>
                    <li><%= link_to "enter info", "/days/new"  %></li>
                    <li><%= link_to "view all days", "/days"  %></li>
                    <li><%= link_to "edit profile", edit_user_registration_path  %></li>
                    <% if !user_signed_in? %>
                      <li><%= link_to "login", "/users/sign_in"  %></li>
                    <% else %>
                      <li><%= link_to("logout", destroy_user_session_path, :method => :delete) %></li>
                    <% end %>
                </ul>
            </div>
           </div>
          </div>
        </div>

      <footer>
        <p>&copy; ben dixon 2012 | <a href="http://www.twitter.com/talkingquickly">@talkingquickly</a> | join <%= number_with_delimiter(rand(1500000), :delimiter => ',').to_s %> others tracking health simply</p>
      </footer>

    </div> <!-- /container -->

   

  </body>
</html>
